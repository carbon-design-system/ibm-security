import { ArgsTable, Story } from '@storybook/addon-docs/blocks';

import { parameters } from '../../../../.storybook/preview';
import Canvas from '../../../../.storybook/components/Canvas';

import anatomy from './images/anatomy.png';

# ButtonCluster

The button cluster group secondary actions that can be taken on a whole page or component. Icons are used to help describe, at a glance, the action that can be taken.

- [Overview](#overview)
- [Component API](#component-api)
- [Grid placement](#grid-placement)
- [Layout considerations](#layout-considerations)
- [Anatomy](#anatomy)
- [Examples](#examples)
- [References](#references)
- [Troubleshooting](#troubleshooting)

## Overview

You can build a button cluster using a combination of the `ButtonCluster` and [`Button`](#references) components. The `ButtonCluster` accepts a list of `Button` components as `children`.

<Canvas>
  <Story
    id="patterns-unstable-layout-modules-buttoncluster--default"
    parameters={parameters}
  />
</Canvas>

## Component API

<ArgsTable />

Additional props passed into `ButtonCluster` will be forwarded along to the underlying button cluster container.

## Grid placement

Button clusters span across 4 columns. Following the [narrow grid](#references) implementation, the layout module container hangs 16 pixels in the left margin. The narrow grid position allows headings and copy outside of containers to align with the labels within the button cluster.

```jsx
<Column lg={4}>
  <ButtonCluster>
    <Button kind="ghost">Button 1</Button>
    <Button kind="ghost">Button 2</Button>
  </ButtonCluster>
</Column>
```

## Layout considerations

Occassionally a spacing token will need to be included to offset the button cluster to ensure it top-aligns with the adjacent layout container. If a top spacing token is necessary, reference the layout module that sits directly above it as the starting point for placement.

## Anatomy

1. **Ghost buttons:** Ghost buttons stack directly below each other
2. **Divider lines:** Divider lines create structurality and provide separation between the ghost buttons
3. **Bottom spacer:** The 16 pixel `$layout-01` below the last button provides the starting point for the layout module positioned directly underneath it

<img alt="`ButtonCluster` anatomy" src={anatomy} />

## Examples

## References

- [Button](https://react.carbondesignsystem.com/?path=/docs/button)
- [Grid](https://react.carbondesignsystem.com/?path=/docs/grid)

## Troubleshooting

Visit our [GitHub](https://github.com/carbon-design-system/ibm-security) to view and submit issues, or reach out on [Slack](https://app.slack.com/client/T1V6D3RT7/CRV39QKP0) for additional support.
