import { parameters } from '../../../../.storybook/preview';

import doImage from './images/do.png';
import dont from './images/dont.png';

import layoutConsiderations from './images/layout-considerations.png';

import anatomy from './images/anatomy.png';

import overview from './images/overview-layout.png';
import detail from './images/detail-layout.png';

import github from './images/github.png';
import slack from './images/slack.png';

# ButtonClusterModule

The button cluster module group secondary actions that can be taken on a whole page or component. Icons are used to help describe, at a glance, the action that can be taken.

[↳ Overview](#overview)  
[↳ Component API](#component-api)  
[↳ Grid placement](#grid-placement)  
[↳ Layout considerations](#layout-considerations)  
[↳ Anatomy](#anatomy)  
[↳ Examples](#examples)  
[↳ References](#references)  
[↳ Troubleshooting](#troubleshooting)

### Overview

The button cluster module can be built using a combination of the `ButtonClusterModule` and [`Button`](#references) components. The `ButtonClusterModule` accepts a list of `Button` components as `children`.

<Canvas>
  <Story
    id="patterns-unstable-layout-modules-buttonclustermodule--default"
    parameters={parameters}
  />
</Canvas>

### Component API

<ArgsTable />

_\*Required property_

Additional props passed into `ButtonClusterModule` will be forwarded along to the underlying button cluster module container.

### Grid placement

Button cluster modules span across 4 columns. Following the [narrow grid](#references) implementation, the layout module container hangs 16 pixels in the left margin. The narrow grid position allows headings and copy outside of containers to align with the labels within the button cluster module.

<Row>
  <Column>
    <img alt="`ButtonClusterModule` do" src={doImage} />
  </Column>

  <Column>
    <img alt="`ButtonClusterModule` don't" src={dont} />
  </Column>
</Row>

```jsx
<Column lg={4}>
  <ButtonClusterModule />
</Column>
```

### Layout considerations

Occassionally a [spacing token](#references) will need to be included to offset the button cluster module to ensure it top-aligns with the adjacent layout container. If a top spacing token is necessary, reference the layout module that sits directly above it as the starting point for placement.

<Row>
  <Column>
    <img
      alt="`ButtonClusterModule` layout considerations"
      src={layoutConsiderations}
    />
  </Column>
</Row>

### Anatomy

1. **Ghost buttons:** Ghost buttons stack directly below each other
2. **Divider lines:** Divider lines create structurality and provide separation between the ghost buttons
3. **Bottom spacer:** The 16-pixel spacer `$layout-01` below the last button provides the starting point for the layout module positioned directly underneath it

<img alt="`ButtonClusterModule` anatomy" src={anatomy} />

### Examples

<Row>
  <Column>
    <LinkTo
      className="storybook__image--link"
      kind="patterns-unstable-modular-page-layouts"
      story="overview"
    >
      <img alt="Overview layout page example" src={overview} />
    </LinkTo>
  </Column>

  <Column>
    <LinkTo
      className="storybook__image--link"
      kind="patterns-unstable-modular-page-layouts"
      story="detail"
    >
      <img alt="Detail layout page example" src={detail} />
    </LinkTo>
  </Column>
</Row>

### References

[↗ Button](https://react.carbondesignsystem.com/?path=/docs/button)  
[↗ Grid](https://react.carbondesignsystem.com/?path=/docs/grid--narrow)  
[↗ Spacing scale](https://carbondesignsystem.com/guidelines/spacing/overview#spacing-scale)

### Troubleshooting

Visit our [GitHub](https://github.com/carbon-design-system/ibm-security) to view and submit issues, or reach out on [Slack](https://app.slack.com/client/T1V6D3RT7/CRV39QKP0) for additional support.

<Row>
  <Column>
    <a className="storybook__image--link" href="https://github.com/carbon-design-system/ibm-security">
      <img alt="Troubleshooting GitHub link" src={github} /> 
    </a>
  </Column>

  <Column>
    <a className="storybook__image--link" href="https://app.slack.com/client/T1V6D3RT7/CRV39QKP0">
      <img alt="Troubleshooting Slack link" src={slack} /> 
    </a>
  </Column>
</Row>
