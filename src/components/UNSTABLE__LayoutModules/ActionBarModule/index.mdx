import { parameters } from '../../../../.storybook/preview';

import doImage from './images/do.png';
import dont from './images/dont.png';

import anatomy from './images/anatomy.png';

# ActionBarModule

Action bars allow users to take multiple actions on the subsequent section.

[↳ Overview](#overview)  
[↳ Variations](#variations)  
[↳ Component API](#component-api)  
[↳ Grid placement](#grid-placement)  
[↳ Anatomy](#anatomy)  
[↳ Examples](#examples)  
[↳ References](#references)  
[↳ Troubleshooting](#troubleshooting)

### Overview

The action bar module can be built using a combination of the `ActionBarModule` and `ActionBarModuleItems` components.

`ActionBarModuleItems` is responsible for positioning the actions, and everything passed in as a child of `ActionBarModuleItems` will be rendered.

<Canvas>
  <Story
    id="patterns-unstable-layout-modules-actionbarmodule--default"
    parameters={parameters}
  />
</Canvas>

### Variations

If necessary, supplementary details can be placed inline with the actions.

<Canvas>
  <Story
    id="patterns-unstable-layout-modules-actionbarmodule--details"
    parameters={parameters}
  />
</Canvas>

### Component API

<ArgsTable />

Additional props passed into `ActionBarModule` and `ActionBarModuleItems` will be forwarded along to their underlying containers.

### Grid placement

Action bar modules can span between 4 and 16 columns, following the [narrow grid](#references) implementation.

<Row>
  <Column>
    <img alt="`ActionBarModule` do" src={doImage} />
  </Column>

  <Column>
    <img alt="`ActionBarModule` don't" src={dont} />
  </Column>
</Row>

```jsx
<Column lg={4}>
  <ActionBarModule />
</Column>
```

### Anatomy

1. **Ghost button:** The predominant action for the bar should be placed here as a ghost button. Alternatively a title can be supplemented, see [Variations](#variations)
2. **Icon buttons:** Nulla et metus quis nulla consequat porttitor.
3. **No spacing:** The action button bar contains internal spacing so it is placed flush to edge of the components above and below

<img alt="`ActionBarModule` anatomy" src={anatomy} />

<Examples />

### References

[↗ Button](https://react.carbondesignsystem.com/?path=/docs/button)

<LinkTo kind="patterns-iconbuttonbar" story="default">
  ↗ IconButtonBar
</LinkTo>

[↗ Grid](https://react.carbondesignsystem.com/?path=/docs/grid--narrow)

<Troubleshooting />
