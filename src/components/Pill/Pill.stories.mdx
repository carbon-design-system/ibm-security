import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { Pill } from '../../';
 
<Meta title="Pillz" component={Pill} />
 
# Pill
 
With `MDX` we can define a story for `Pill` right in the middle of our
markdown documentation.
 
<Preview>
  <Story name="all Pilles">
    <form>
      <Pill value='127.0.0.1' type='IP' />
    </form>
  </Story>
</Preview>