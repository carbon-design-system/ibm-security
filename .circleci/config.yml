# https://circleci.com/docs/2.0/language-javascript

jobs:
  build:
    docker: circleci/node:11
    #  - image: circleci/node:11
    steps:
      - checkout
      - yarn install --offline --frozen-lockfile
      - scripts/ci/build.sh
      - yarn test
      - yarn semantic-release
