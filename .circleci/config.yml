# https://circleci.com/docs/2.0/language-javascript

jobs:
  build:
    docker:
      - image: circleci/node:11
    steps:
      - checkout
      - run: yarn install --offline --frozen-lockfile
      - run: scripts/ci/before_script.sh
      - run: yarn semantic-release
